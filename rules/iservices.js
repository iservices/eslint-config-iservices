module.exports = {
  'rules': {
    // indents are 2 space characters.
    "indent": [2, 2],
    // no commas after the last item in list
    "comma-dangle": [2, "never"],
    // must have valid jsdoc
    "valid-jsdoc": 1,
    // don't require strict directives.
    "strict": 0
  }
};
